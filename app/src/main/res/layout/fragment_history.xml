<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@color/primary"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/spacing_md">

        <TextView
            android:id="@+id/tvHistoryTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Workout History"
            android:textColor="@color/text_on_primary"
            android:textSize="@dimen/text_lg"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Search Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/background"
        android:orientation="horizontal"
        android:padding="@dimen/spacing_md"
        android:gravity="center_vertical">

        <EditText
            android:id="@+id/etSearchWorkout"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:background="@drawable/bg_search_input"
            android:hint="Search by workout name"
            android:paddingStart="@dimen/spacing_md"
            android:paddingEnd="@dimen/spacing_md"
            android:inputType="text"
            android:textSize="@dimen/text_md"
            android:textColorHint="@color/text_secondary" />

        <Button
            android:id="@+id/btnClearSearch"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_marginStart="@dimen/spacing_sm"
            android:text="Clear"
            android:textSize="@dimen/text_sm"
            android:paddingHorizontal="@dimen/spacing_md" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="@dimen/spacing_md"
        android:paddingBottom="@dimen/spacing_sm">

        <EditText
            android:id="@+id/etSessionDate"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginBottom="@dimen/spacing_sm"
            android:background="@drawable/bg_search_input"
            android:hint="Date (e.g. Jun 5)"
            android:inputType="text"
            android:paddingHorizontal="@dimen/spacing_md" />

        <EditText
            android:id="@+id/etSessionWorkout"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginBottom="@dimen/spacing_sm"
            android:background="@drawable/bg_search_input"
            android:hint="Workout Name"
            android:inputType="text"
            android:paddingHorizontal="@dimen/spacing_md" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <EditText
                android:id="@+id/etSessionDuration"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_marginEnd="@dimen/spacing_sm"
                android:layout_weight="1"
                android:background="@drawable/bg_search_input"
                android:hint="Duration (e.g. 50m)"
                android:inputType="text"
                android:paddingHorizontal="@dimen/spacing_md" />

            <EditText
                android:id="@+id/etSessionVolume"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:background="@drawable/bg_search_input"
                android:hint="Volume kg"
                android:inputType="text"
                android:paddingHorizontal="@dimen/spacing_md" />
        </LinearLayout>

        <Button
            android:id="@+id/btnAddSession"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="@dimen/spacing_sm"
            android:text="Add Session (Create)" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvHistory"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:padding="@dimen/spacing_md" />

</LinearLayout>
